# Movila

This is the root of the Movila project, containing both the backend (Node.js/Express) and mobile (React Native/Expo) applications.

## Structure
- `backend/` — Node.js/Express REST API
- `mobile/` — React Native (Expo) mobile app

## Getting Started
See the README files in each subfolder for setup instructions.
